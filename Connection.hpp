#pragma once

#include "Request.hpp"
#include "Response.hpp"

// class Connection
// {
//     private:
//         // int _socket;
//         // Request _request;
//         // Response _response;
//         // bool _isComplete;
    
//     public:
//         Connection(int sock) : _socket(sock), _isComplete(false) {};
//         // bool close()  const;
//         // void sockRead() const;
//         // void sockWrite() const;
// };